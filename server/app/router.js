"use strict";

/**
 * @param {Egg.Application} app - egg application
 */
module.exports = (app) => {
  const { router, controller } = app;
  router.get("/", controller.home.index);
  router.get("/query", controller.home.query_earthquake);
  router.get("/predict", controller.home.predict);
};
